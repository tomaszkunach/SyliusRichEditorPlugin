{#
    UI Element template
    type: title
    element fields :
        content
        align
#}
{% set align = element.align is defined and element.align is not empty ? element.align : 'inherit' %}
{% if element is not empty %}
    <div class="h1" style="text-align: {{align}};">{{ element.content }}</div>
{% endif %}
